# ===== PostgreSQL =====
spring.datasource.url=jdbc:postgresql://localhost:5432/repairdb
spring.datasource.username=repairuser
spring.datasource.password=repairpass
spring.datasource.driver-class-name=org.postgresql.Driver

# ===== JPA / Hibernate =====
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.open-in-view=false

# ===== Загрузка файлов =====
app.upload.dir=uploads
spring.servlet.multipart.enabled=true
spring.servlet.multipart.max-file-size=50MB
spring.servlet.multipart.max-request-size=50MB

# ===== Кодировка =====
server.servlet.encoding.charset=UTF-8
server.servlet.encoding.enabled=true
server.servlet.encoding.force=true

# ===== Thymeleaf =====
spring.thymeleaf.encoding=UTF-8
spring.thymeleaf.cache=false

# ===== Локаль =====
spring.mvc.locale=ru_RU
spring.mvc.locale-resolver=fixed

# ===== Отправка SMS через Email =====
# Раскомментируйте и заполните если нужна отправка SMS:
# spring.mail.host=smtp.yourserver.com
# spring.mail.port=587
# spring.mail.username=your-email@example.com
# spring.mail.password=your-password
# spring.mail.properties.mail.smtp.auth=true
# spring.mail.properties.mail.smtp.starttls.enable=true
# spring.mail.properties.mail.smtp.connectiontimeout=5000
# spring.mail.properties.mail.smtp.timeout=5000
# spring.mail.from=repair-system@example.com
